<div class="logo">
<%= image_tag "bookworm_sm.png" %>
<%= link_to "Bookworm", root_path, :class => "brand logo" %>
</div>
<ul class="nav">
<li>
    <%= form_tag("/search", method: "get", :class => "navbar-form search") do %>
      <%= text_field_tag :query, "", :placeholder => "Title, author or ISBN", autofocus: true, :class => "span2" %>
      <%= submit_tag "Search",:name => nil, :class => "search btn btn-mini" %>
    <% end %>
  </li>
</ul>
<ul class="nav pull-right">
  <% if user_signed_in? %>
    <li class="dropdown">
      <a href="#" data-toggle="dropdown" class="dropdown-toggle" >Hi, <%= current_user.first_name %></a>
      <ul class="dropdown-menu">
        <li>
          <%= link_to("My Bookbag", bookbag_path) %>
        </li>
        <li>
          <%= link_to("Edit account", edit_user_registration_path) %>
        </li>
        <li>
          <%= link_to("Logout", destroy_user_session_path, :method => "delete") %>
        </li>
      </ul>
    </li>
  <% else %>
  <!-- User not signed in -->
    <li>
      <%= link_to("Login", new_user_session_path) %>
    </li>
  <% end %>
  <% if !user_signed_in? %>
    <!-- <li>
      <%#= link_to("My Bookbag", edit_user_registration_path) %>
    </li>
    <li>
      <%= link_to("Edit account", edit_user_registration_path) %>
    </li>
    <li>
      <%= link_to("Logout", destroy_user_session_path, :method => "delete") %>
    </li>
  <%# else %> -->
  <!-- user not signed in -->
    <li>
      <%= link_to("Sign up", new_user_registration_path) %>
    </li>
  <% end %>
  <li>
    <%= link_to("Contact Us", "/contact") %>
  </li>
</ul>